# TODO: Setup scripts
# Symlinking the dotfiles to the home directory
# use [ ln -s [original] [link] ] to link

echo "unimplemented"

# Install texdef
mkdir ~/texdef
git clone https://github.com/MartinScharrer/texdef ~/texdef
sudo ln 
